import { Speed } from './response-spped.dto';
import { VolumetricFlow } from './response-volumetric-flow.dto';

export class ResponseCalculateFlowAtmospheric {
  speed: Speed;
  volumetricFlow: VolumetricFlow;
}
